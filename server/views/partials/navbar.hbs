<nav class="navbar navbar-expand-lg navbar sticky-top" style="background-color: #333333;">
	<a class="navbar-brand" href="/"></a>
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
		aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>

	<div class="collapse navbar-collapse justify-content-between" id="navbarSupportedContent">
		
	</div>
	
	<!--<div>
		<ul class="navbar-nav">
			<li class="nav-item position-static" style="padding-left:10px;">
				<a class="nav-item dropdown-toggle" role="button" data-toggle="dropdown">
					<i class="far fa-bell" id="Trigger"></i>
				</a>
				<div class="dropdown-menu w-80" style="right: 0">
					 <a>{{{minimums}}}</a>
				</div>
              
			</li>
		</ul>
	</div>-->
	<div class="icon" id="bell"><span class="fa-stack"><i class="far fa-bell fa-lg fa-stack-1x"></i> {{#unless (isdefined minimums "Satisfactory Stock Levels")}}<i class="fas fa-certificate fa-xs fa-stack-1x" style="color:Tomato"></i> {{/unless}}</span></div>
	<div class="notifications" id="box">
		<h2>Stock Deficits</h2>
		<div class="notifications-item">
			<div class="text">
				<p>{{{minimums}}}</p>
			</div>
		</div>
	</div>
	<div class="navbar-nav">
		<ul class="navbar-nav">
			<li class="nav-item dropdown">
				<a class="nav-link dropdown-toggle" href="#" id="userInfoNav" role="button" data-toggle="dropdown"
					aria-haspopup="true" aria-expanded="false">
					{{user.id}}
					&nbsp;
					{{console user}}
					<img src="/profilePictures/{{user.profilePicture}}" width="40" height="40"  style='object-fit:contain;' class="rounded-circle">
				</a>
				<div class="dropdown-menu dropdown-menu-right" aria-labelledby="userInfoNav">
					<a class="dropdown-item" id="editAccount" href="/users/edit/{{user.id}}">Edit Account</a>
					<a class="dropdown-item" href="/users/display-user/{{user.id}}" id="viewAccount"><i class="far fa-user"></i> View Account</a>
					<a class="dropdown-item" href="/users/update/{{user.id}}" id="updateAccount"><i class="fas fa-cog"></i> View Jobs</a>
					<a class="dropdown-item" id="logout" href="/logout"><i class="fas fa-sign-out-alt"></i> Logout</a>
				</div>
			</li>
		</ul>
	</div>
</nav>

{{#if breadcrumbs}}
	{{!-- Breadcrumb --}}
	<nav aria-label="breadcrumb">
		<ol class="breadcrumb">
			{{#each breadcrumbs}}
				<li class="breadcrumb-item"><a href="{{this.url}}">{{this.name}}</a></li>
			{{/each}}
		</ol>
	</nav>

	<style>
		.breadcrumb {
			margin-top: -2rem;
		}
	</style>
{{/if}}
