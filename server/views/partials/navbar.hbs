<nav class="navbar navbar-expand-lg navbar sticky-top" style="background-color: #333333;">
	<a class="navbar-brand" href="/"></a>
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
		aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>

	<div class="collapse navbar-collapse justify-content-between" id="navbarSupportedContent">
		
	</div>
	
	<!--<div>
		<ul class="navbar-nav">
			<li class="nav-item position-static" style="padding-left:10px;">
				<a class="nav-item dropdown-toggle" role="button" data-toggle="dropdown">
					<i class="far fa-bell" id="Trigger"></i>
				</a>
				<div class="dropdown-menu w-80" style="right: 0">
					 <a>{{{minimums}}}</a>
				</div>
              
			</li>
		</ul>
	</div>-->
	<ul class="navbar-nav ms-auto d-flex flex-row">
    <!-- Notification dropdown -->
    <li class="nav-item dropdown">
      <a
        class="nav-link me-3 me-lg-0 hidden-arrow"
        href="#"
        id="bell"
        role="button"
        data-toggle="dropdown"
        aria-expanded="false"
        >
        <span class="fa-stack">
				  <i class="far fa-bell fa-lg fa-stack-1x"></i> 
				  {{#unless (isdefined minimums "Satisfactory Stock Levels")}}
				    <i class="fas fa-certificate fa-xs fa-stack-1x" style="color:Tomato"></i> 
				  {{/unless}}
			  </span>
      </a>
      <ul
        class="dropdown-menu dropdown-menu-right"
        aria-labelledby="bell"
        >
        <li>
          <a class="dropdown-item" href="#">
            <h2>Stock Deficits</h2>
				    <p>{{{minimums}}}</p>
          </a>
        </li>
      </ul>
    </li>

    <!-- Avatar -->
    <li class="nav-item dropdown" style="color: black;">
      <a
        class="nav-link dropdown-toggle hidden-arrow d-flex align-items-center"
        href="#"
        id="userInfoNav"
        role="button"
        data-toggle="dropdown"
        aria-expanded="false"
        >
        {{user.id}}
        &nbsp;
			  {{console user}}
        <img
          src="/profilePictures/{{user.profilePicture}}"
          class="rounded-circle"
          height="40"
          alt=""
          loading="lazy"
        />
      </a>
      <ul
        class="dropdown-menu dropdown-menu-right"
        aria-labelledby="userInfoNav"
        >
        <li><a class="dropdown-item" id="editAccount" href="/users/edit/{{user.id}}">Edit Account</a></li>
        <li><a class="dropdown-item" href="/users/display-user/{{user.id}}" id="viewAccount"><i class="far fa-user"></i> View Account</a></li>
        <li><a class="dropdown-item" href="/users/update/{{user.id}}" id="updateAccount"><i class="fas fa-cog"></i> View Jobs</a></li>
        <li><a class="dropdown-item" id="logout" href="/logout"><i class="fas fa-sign-out-alt"></i> Logout</a></li>
      </ul>
    </li>
  </ul>
</nav>

{{#if breadcrumbs}}
	{{!-- Breadcrumb --}}
	<nav aria-label="breadcrumb">
		<ol class="breadcrumb">
			{{#each breadcrumbs}}
				<li class="breadcrumb-item"><a href="{{this.url}}">{{this.name}}</a></li>
			{{/each}}
		</ol>
	</nav>

	<style>
		.breadcrumb {
			margin-top: -2rem;
		}
	</style>
{{/if}}
